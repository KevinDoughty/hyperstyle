import { dimensionType } from "../../hyper-animations.mjs";
// console.log("this is not going to work because variables appear in the wrong order");
const dimension = dimensionType();
console.log("dimension type:",dimension);
